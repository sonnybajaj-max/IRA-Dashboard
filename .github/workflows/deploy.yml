name: Generate Playbook Dashboard

on:
  push:
    branches:
      - main  # Or 'master', depending on your default branch name
  workflow_dispatch: # Allows you to manually click "Run Workflow" in GitHub

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas pypdf

      - name: Execute Dashboard Script
        run: |
          # Finds your uploaded CSV and passes it to dashboard.py
          python dashboard.py Portfolio_Positions_Jul-24-2026.csv

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .  # Publishes the generated index.html
